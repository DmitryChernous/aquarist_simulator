# ROADMAP — Доработки (по одной в релиз/коммит)

Порядок важен: более ранние пункты — фундамент для поздних.
Каждый пункт = отдельный коммит + тег + поднятие версии (package.json,
src/version.ts, строка в «Журнал версий» PLAN.md).

Соглашения-термины (общие для всего проекта, всегда консистентно):
- «Стойка» — аквариумный стеллаж (state.shelves), держит аквариумы.
- «Стеллаж» — хранилище предметов (state.racks: оборудование и декор), размещается
  в помещении как объект; каждый стеллаж даёт +STORAGE_RACK_CAPACITY мест.
- «Витрина» (displayRack) — мебель в зале: показывает покупателям, какие позиции
  есть; НЕ ограничивает продажи и НЕ даёт вместимость склада. Продажа идёт со склада напрямую.

---------------------------------------------------------------
## 0.10.0 — №3: Система размещения аквариумов на стойках — ВЫПОЛНЕНО
Проблема: на одну полку (slab) помещается максимум 1 аквариум — неважно какого
размера. Нужно несколько аквариумов в ряд по фактической ширине.

Файлы: src/types.ts, src/data/shop.ts, src/sim/aquarium.ts, src/main.ts,
       src/ui/panels.ts, src/ui/renderHall.ts, src/save.ts.

Изменения:
- AquariumState + поле x (позиция в см от левого края полки; по умолч. 0). В loadState
  добавить a.x ?? 0 (без полной миграции).
- Новая функция упаковки (напр. placeOnShelf(shelf, model, qty)) в sim/aquarium.ts:
  по полкам (по порядку slot) считает свободные горизонтальные промежутки между
  аквариумами, ставит аквариум, если в промежутке хватает ширины И slab.depth/height
  подходят И volume <= shelfLoadLeft. Возвращает [{slabId, x}, ...] или null.
- main.ts: onAddAquarium(shelfId,modelId) → onBuyAquariums(modelId, shelfId, qty)
  (списывает деньги, создаёт qty аквариумов с x через упаковку, 1 bump, общий лог).
- renderHall.drawShelf/drawTank: аквариумы рисуются по x, масштабу w от края полки
  (а не на всю полку).
- panels.openAddAquariumModal: пересчёт «помещается» через новую упаковку.
- Удалить/заменить freeSlabFor.

Критерий: на полку 120 см помещается 2 аквариума по 60 (или 60+40), рисуются рядом
и не вылезают; грузоподъёмность и габариты учитываются.

---------------------------------------------------------------
## 0.11.0 — №1: Покупка аквариумов в магазине: модалка + количество — ВЫПОЛНЕНО
Проблема: в «Магазин»→«Обустройство»→«Аквариумы» в карточке выпадающий список со
стойками (неудобно). Нужно: кнопка «Купить» → модалка с выбором стойки и количества.

Файлы: src/ui/panels.ts, src/main.ts (действие из 0.10.0).

Изменения:
- renderFurnStore (раздел аквариумов): убрать select из карточки; кнопка
  «Купить — цена₽/шт».
- openBuyAquariumModal(state, model): по каждой стойке — число/stepper количества,
  макс = сколько влезет этой модели на стойку (через 0.10.0); итоговая цена;
  кнопка «Купить». Причины недоступности (нет стоек / не хватает денег / полка занята).
- onBuyAquariums списывает и размещает выбранное количество.

Критерий: можно купить несколько аквариумов сразу на конкретную стойку с учётом
вместимости; выпадающего списка в карточке больше нет.

---------------------------------------------------------------
## 0.12.0 — №2: Защита от перерисовки выпадающих списков — ВЫПОЛНЕНО
Проблема: store-панель перерисовывается на каждый bump (1 раз/сек), live-select в
карточках сбрасывался к 1-му пункту — нельзя выбрать другой.

Файлы: src/ui/panels.ts.

Изменения:
- fillSelect: до пересборки запомнить текущее выбранное значение; после — восстановить,
  если оно ещё есть в новом списке (не сбрасывать на unrelated re-render).
- Правило: интерактивные select размещаются в модалках/панелях с own-рендером, а не в
  авто-перерисовываемых store-карточках (зафиксировать в ROADMAP).
- Пункт 0.11.0 уже убрал проблемный select; здесь страховка на будущее + кросс-проверка
  остальных select (modals, aquariumSelect, sort/filter).

Критерий: ни один select не сбрасывается на фоновый bump; можно выбрать любой пункт.

---------------------------------------------------------------
## 0.13.0 — №6: Один склад; вместимость от стеллажей — ВЫПОЛНЕНО
Проблема: несколько складов (state.storage[]) и выбор склада в магазине и на складе;
вместимость = (полки комплектующих + витрины)×8.

Новая модель:
- Склад всегда один (один TankState).
- Единственная сущность хранения — «Стеллаж» (новое поле shop.storageRacks; миграция
  из componentRacks, старт 1). displayRack больше НЕ даёт слоты хранения.
- Макс. вместимость склада — константа STORAGE_MAX_SLOTS = 500 (тестовая; подберём позже).
- Вместимость стеллажа STORAGE_RACK_CAPACITY = 40.
- Текущая вместимость = min(STORAGE_MAX_SLOTS, storageRacks * 40).

Файлы: src/types.ts, src/data/shop.ts, src/main.ts, src/ui/panels.ts,
       src/ui/renderHall.ts (storage-объекты), src/save.ts, src/sim/*.

Изменения:
- ShopState.componentRacks → storageRacks (миграция в save.ts: componentRacks→storageRacks;
  из нескольких складов слить stock в один).
- state.storage: массив → единый TankState (обновить addStock/takeStock/tryArrive/
  onMoveToStorage/onBuyFishToStorage/onWholesaleSell/renderStorage/renderStore).
- Убрать storageSelect + карточки «Склады» + кнопку «Добавить склад»; убрать storeDest
  (select «Купить в склад:» в «Для продажи»).
- Экран склада: кнопка «Купить стеллаж — 150₽» (+40 вместимости, до 500). В магазине
  «Обустройство»→«Оснащение» карточка «Полка комплектующих» → «Стеллаж» (тоже покупается).
- Тексты/подсказки «полка комплектующих»/«стеллаж-витрина вместимость» → «стеллаж».
- renderHall.layoutStorageObjects: стеллажи рисуются по storageRacks.
- Продажа со склада напрямую уже так работает (заказы смотрят весь rackInventory/
  rackDecor) — витрина не ограничивает продажу, сохранить.

Критерий: один склад без выбора; вместимость растёт от числа стеллажей (покупка в
магазине И на экране склада), не выше 500; продажа всех товаров напрямую со склада.

---------------------------------------------------------------
## 0.14.0 — №5: Убрать hall-list; детали по стойкам — через кнопку/модалку — ВЫПОЛНЕНО
Проблема: экран помещений перегружен детальной инфой по каждой стойке (hall-list /
shelf-card).

Изменения (детали — при реализации, как договорились):
- Полностью убрать блок hall-list (renderZal, участки карточек стоек) и его DOM.
- На экране «Помещения» добавить кнопку (место определим позже; кандидат — «Управление
  стойками» в hall-actions), вызывающую модалку со сводкой/деталями по стойкам
  (помещения, полки, аквариумы, действие «добавить», «переместить», «продать»).
- Канвас зала остаётся основным визуальным представлением.

Файлы: src/ui/panels.ts, src/ui/renderHall.ts (если нужно), src/main.ts.

Критерий: на экране помещений нет развёрнутой информации по стойкам; детали — по
нажатию кнопки в модалке.

---------------------------------------------------------------
## 0.15.0 — №4: Различать аквариумы в выпадающем списке на экране «Аквариум» — ВЫПОЛНЕНО
Проблема: аквариумы нумеруются в пределах стойки («Аквариум 1» повторяется), по списку
непонятно, с какой стойки выбран.

Файлы: src/ui/panels.ts (renderAquarium).

Изменения:
- В renderAquarium label пункта включает место: `<icon> <помещение> · <стойка> · <имя>`
  (через shelfOfAquarium). Без аквариума на стойке — только имя.

Критерий: в выпадающем списке каждый аквариум однозначно идентифицирован по стойке.

---------------------------------------------------------------
## 0.16.0 � �2: ������ ������������ �� ����� �������
��������: ������� ������� / ������� / ����� / ������� ������� ���������� �
������������� ����� ��������.

����������� (���������� � types.ts � ���������� ����� ���������):
- ������� � ����������� ������� (state.shelves): ������ ���������.
- �������� � ��������� ��������� (state.racks): ������������ � �����.
- �������� (displayRack) � ������ � ���� ��� ������������ ������������ �����������.

���������:
- �������� �� ���� ������� UI (panels.ts, renderHall.ts, main.ts): ������
  ��������-�������>��������, ������ ������� ������ ��������������>��������.
- �����������-������� � ����� ��������� � types.ts (ShopState).

�����: src/types.ts, src/ui/panels.ts, src/ui/renderHall.ts, src/main.ts.

��������: �� ���� ��������/���������� ���������� ������� �� ������.

---------------------------------------------------------------
## 0.17.0 � �4: ������ tank-bar �� ������� �������������
��������: � ��������>������������� ������ tank-bar (furnBar) � �������
������������� ��������: ������, ��������� � ������� � ������, ��� �����������.

���������:
- ������� furnBar (tank-bar) � ��� ����� � panels.ts (������ furnGroup).
- ��������� CSS .store-group .tank-bar, ���� ��� �� �� ������ ����� �� �����.
- tank-bar �� ������ ��������� (aqBar) �� �������.

�����: src/ui/panels.ts, src/style.css.

��������: � ������������� ��� ������� ������-���������.

---------------------------------------------------------------
## 0.18.0 � �5: ������ ��������� comp-hint � ���� � ���������� �������
��������: comp-hint � ������� ����� ��� ��������� �����/������ ����� � �������
������� �������� � �������� �������.

���������:
- ������� comp-hint, ����������� ����� � ����� �������� (furnShelves, furnAq � ��.).
- �������� comp-hint ������ �������/��������� ���� (��� ��� ����� ����: �����������,
  ����������, �����������������). ����������� �������� ������� �����.

�����: src/ui/panels.ts.

��������: � ������ �������� ��� ��������� ��������� ����� ����������.

---------------------------------------------------------------
## 0.19.0 � �3: ������� � ������ � ��������; ���������� ���������� ������/��������
��������: ������ ���������� �� ����� � ����� �������� �����������; ������� � ������
������� storageRacks ��� �������. �������: ��� ���������� � ������ ��������� ������
����� �������� � ��� ���������, � ��� ������� ����� ������� � ����� �������.

����� ������:
- state.racks: { id, roomId }[] ������ ShopState.storageRacks: number.
  ����������� = min(STORAGE_MAX_SLOTS, racks.length * STORAGE_RACK_CAPACITY).
  ��������: ������ storageRacks N > N �������� ��������� � ��������� ������.
- ������� ������/�������� � ��������>�������������: ������� � ������� ������� >
  ������ �������� ����� � ��������� �������.
- ����� �����������: ��������� ������ / ��������� ������� � ������ ������ � �������
  ������� (viewRoom) �����.
- ������ shelvesInventory � ����� ������ > ���������� �����: onPlaceShelf,
  onSellInventoryShelf, openPlaceShelfModal, openAddShelfModal(������) � �������.
- renderHall.layoutStorageObjects: �������� �������� �� state.racks, ���������������
  �� viewRoom; ������� ��� ��������.
- buyers.ts: ����������������� �� ���������� ��������� racks.length*3.

�����: src/types.ts, src/data/shop.ts, src/main.ts, src/ui/panels.ts,
       src/ui/renderHall.ts, src/save.ts, src/sim/buyers.ts.

��������: ������/������� ����� � ������ ������� ��� ������� ��� �� ����������;
��������� ������ ����� ���.

---------------------------------------------------------------
## 0.20.0 � �1: ����� ������ ��� storage-block; ��������� ������ � ��������
��������: storage-block (������ �� ������, ���� �� �������, ����� ��������) ����������
� �����. ����� ������ � ������ ����� ������ � ����������� � �������� ��� � �����.

���������:
- ������ storageBlock (shelf-inventory + stock-list + rack-panel) �� zalTab.
- ����� ������: �������������� � ������ �����, ������ ������ (������ ���
  viewRoom === 'storage').
- ������: ����������� (���� / ������ / �������� + ��������-���) �����
  storageCapacity/storageUsed; ��������� � ���������-�������� ���� | ���� |
  ������������ | �����, ������������ state.storage.stock (����) +
  shop.rackInventory (������������) + shop.rackDecor (�����); �������� (��� ��� ���).
- ������-������ ������� ������� ���� � �������������>����������.

�����: src/ui/panels.ts, src/style.css.

��������: �� ������� ��� storage-block; ������ ������ � ������������ � ��������� ��������.

---------------------------------------------------------------
## 0.21.0 � �6: ����������� ������� (dirty-flag, ������ ������ �������)
��������: setInterval(() => bump(), 1000) ��� � ������� ������������ ��� ������
(innerHTML=''); HUD ����������� ������ ����. � DOM � ��������� ��������, ������ ������.

���������:
- ������ �������������� �������� ��� � ������� (setInterval(bump,1000)).
- HUD: ���������� ��������� ������ (������/����/�����������������/������/�������),
  ������ ������ ��� ���������; ��������-��� ��� �������������� (~500 ��).
- Per-��������� ���������� (fingerprint): ������ render*-������� ������������ ����
  ������ ������ ���� ���������� ������, ������� ��� ���������� (����� ����������).
- ��������� ������ ������ ������� �������; ������� ������ �� ������������.
- ��� � append-only; ������ ������� ������������ ��� ��������� �������� timeLeft/������.
- ������� (���/��������) ���������� ���������� ������ ���� (��� ��������, �� DOM).

�����: src/main.ts, src/ui/panels.ts.

��������: � ������� (������ �� ��������) ������ �� ��������������; ����������� ����
HUD � ������; ������� ������ ��������� DOM � ���������.

---------------------------------------------------------------
## 0.22.0 - №1+2: Стандартизация layout экранов (помещения + аквариум) - (завершено)
Проблема: экраны выглядят по-разному. У «Зала»/«Разводни» нет информационной панели
(как у «Склада»), канвас был отцентрирован; «Аквариум» имел отдельный layout.

Изменения:
- Общий room-shell: .room-layout / .room-stage / .room-aside.
  Канвас помещений прижат к левому краю; aside справа с информацией о текущем
  помещении (renderRoomAside): Зал - стойки/аквариумы/рыбы/мебель/привлекательность/
  продажи/посетители; Склад - прежняя панель инвентаря и вместимости; Разводня -
  базовые счётчики + «в разработке». hall-actions остаются под канвасом.
- «Аквариум» переведён на тот же room-shell: tank-bar сверху, канвас по центру,
  aside «Состояние» справа, действия (aq-actions) снизу. Удалены aq-body и его CSS.
- fpZal расширен данными aside (для авто-обновления при событиях).

Файлы: src/ui/panels.ts, src/style.css, src/sim/buyers.ts (импорт).

Критерий: все три помещения и аквариум используют единый скелет (канвас слева,
aside справа, действия снизу); канвас у левого края.

---------------------------------------------------------------
## 0.23.0 - №3: Магазин: фильтры, сортировка и категории - (в работе)
Проблема: каталог без фильтров - при росте ассортимента тяжело искать; нет группировки по семействам/регионам/категориям.

Изменения:
- Метаданные каталога (только к текущим 10/6/4, без новых товаров):
  FishSpecies += family, region; EquipmentDef += category (filter|pump|light|heater|co2|measure) + power (Вт); DecorDef += group.
- .store-controls над обеими группами магазина: поиск + select сортировки + facet-чипы.
- Рыбы (Для продажи): фильтры Семейство/Регион, сортировка имя/цена↑↓/привлекательность/размер, группировка по семейству/региону со sticky-заголовками; бейджи family+region на карточке.
- Декор: фильтр по группе, сортировка цена/привлекательность/имя; бейдж группы.
- Оборудование: фильтр по категории, сортировка цена/мощность/имя; бейдж категории + мощность Вт.
- Обустройство: общая сортировка (имя/цена) для остальных разделов.

Файлы: src/types.ts, src/data/fish.ts, src/data/shop.ts, src/data/decor.ts, src/ui/panels.ts, src/style.css.

Критерий: в магазине можно отфильтровать по семейству/региону/группе/категории, отсортировать и сгруппировать; карточки показывают бейджи и мощность; пустой результат - «Ничего не найдено».

---------------------------------------------------------------
## 0.24.0 - Пакет фиксов: канвас зала, вместимость помещений, названия аквариумов, селектор, переход в магазин
Проблема: надпись-заголовок на канвасе дублировала название; у зала не было лимита стоек (стойки «уезжали» за край); при покупке нескольких аквариумов им присваивалось одно имя; aquariumSelect не реагировал на выбор; из модалки «Обитатели» не было быстрого перехода в магазин.

Изменения:
- renderHall: убран заголовок «icon имя — описание» с канваса (зал читается по канвасу и aside).
- Вместимость помещения: каждая стойка = 2 базовые ячейки + 1 за полку + зазор; лимит ROOM_SHELF_CELL_CAPACITY = 18 (ширина зала). Блок в main.ts (onBuyShelf/onMoveShelf) при переполнении; в openBuyRoomModal недоступные помещения помечаются «Нет места»; в aside зала показывается «ячейки X/18»; layoutHall сжимает стойки (scale), если их больше лимита (старые сохранения) — ничего не выходит за край.
- main.onBuyAquarium: аквариумы создаются и добавляются по одному — названия «Аквариум N» уникальны на стойке.
- panels.fillSelect: добавлен параметр preferCurrent; aquariumSelect заполняется с ним — селектор всегда отражает state.selectedAquariumId.
- Модалка «Обитатели»: кнопка «Купить рыб в магазине →» (закрывает модалку, switchTab(store) + setStoreMode('sale') + setStoreSection('fish')).

Файлы: src/ui/renderHall.ts, src/main.ts, src/ui/panels.ts.

Критерий: стойки не выходят за край канваса и покупка блокируется при заполненном помещении; аквариумы в одной покупке имеют разные имена; селектор аквариума переключает экран; из «Обитателей» можно попасть в магазин за рыбой.

---------------------------------------------------------------
## 0.25.0 - Ручные названия аквариумов
Проблема: при большом количестве стоек и аквариумов игроку сложно запомнить, где какой «выставочный» аквариум (например, цихлидник Южной Америки) — только номера «Аквариум N».

Изменения:
- main.ts: onRenameAquarium(aqId, name) — trim, лимит 40 символов, лог, bump (перерисовка).
- panels.ts: UIActions.onRenameAquarium; модалка openRenameModal (input, Enter/Сохранить, автоподсказка); кнопки «Переименовать» на экране аквариума (рядом с названием в aside) и в меню стойки (список «На стойке»).
- Имя аквариума уже показывается на канвасе (drawTank), в селекторе, в списках и логах — везде сразу подхватывается.

Файлы: src/main.ts, src/ui/panels.ts, src/style.css.

Критерий: можно переименовать аквариум с экрана аквариума или из меню стойки; новое имя отображается на канвасе, в селекторе и списках; пустое имя не сохраняется.

---------------------------------------------------------------
## 0.25.1 - Инлайн-переименование аквариума
Вместо модального окна добавил редактирование прямо на экране аквариума.

Изменения:
- На экране аквариума в aside справа от названия (aq-name-row) — иконка ✎.
- По клику aq-keys превращается в инлайн-инпут; весь текст сразу выделен (ввод нового заменяет старое).
- На месте ✎ появляются иконки ✓ (сохранить) и ✕ (отменить); работают также Enter и Esc.
- Фокус и позиция курсора сохраняются при перерисовке aside по тикам (хелпер renderAqNameRow + editSelStart/End).
- Пустое имя при сохранении игнорируется (не стирает текущее).

Файлы: src/ui/panels.ts, src/style.css (.icon-btn, .rename-inline).

Критерий: можно переименовать аквариум без модалки; текст выделен при старте; подтверждение/отмена; фокус не слетает при тиках.

---------------------------------------------------------------
## 0.25.2 - Не скрывать остальной контент при переименовании

Изменения:
- Название аквариума живёт в отдельном постоянном контейнере aq-name-box (первый ребёнок aq-info).
- renderAquarium больше не делает aqInfo.innerHTML='' — убираются и пересобираются только узлы после aq-name-box.
- renderAqNameRow не пересоздаёт инпут, если уже редактируется этот же аквариум (guard по editInput) — фокус и курсор не слетают.
- При смене выбранного аквариума или «не выбран» состояние редактирования сбрасывается.

Файлы: src/ui/panels.ts.

Критерий: при редактировании названия остальная информация (вода, обитатели, оборудование, декор) остаётся видимой и продолжает обновляться, без мигания.

---------------------------------------------------------------
## 0.25.3 - Стабильная ширина при скроллбаре
Проблема: при переходе между экранами менялась высота контента, из-за чего то появлялся, то исчезал вертикальный скроллбар страницы; классический скроллбар Windows съедает ~15px ширины, и интерфейс (например time-control в hud) перепрыгивал.

Изменения:
- html { scrollbar-gutter: stable } — браузер всегда резервирует место под скроллбар, ширина доступного контента не меняется.

Файлы: src/style.css.

Критерий: появление/исчезание скроллбара не сдвигает контент и не меняет его ширину.

---------------------------------------------------------------
## 0.26.0 - Корм: покупка, хранение, расход
Раньше кормление просто снимало 5₽ глобально. Теперь нужен реальный корм: его покупают, хранят, расходуют, и разные корма подходят разным рыбам.

Классификация кормов:
- Сухие (dry): хлопья, гранулы средние/крупные, таблетки, спирулина, палочки — не портятся.
- Живые (live): артемия, мотыль, дафния — сытнее, но портятся (shelfLifeDays) и требуют холодного хранения.

Как это работает:
- У рыб появились diet (carnivore/herbivore/omnivore) и mouthSize (small/medium/large). Корм съедает рыба, которой он подходит по размеру частиц (не крупнее рта) и диете.
- Новый раздел «Корм» в магазине («Для продажи»): карточки, фильтр по типу (сухие/живые) и размеру (мелкий/средний/крупный), сортировка по цене/порциям/сытости/имени.
- Покупка банки добавляет jarPortions порций на склад (state.shop.foodStock).
- Кормление (модалка openFeedModal) тратит 1 порцию и повышает сытость только тех рыб, кому корм подходит; не денег.
- Живые корма стареют каждый день и портятся (advanceFoodSpoilage), записи удаляются с логом.
- Склад показывает запасы корма и свежесть живых.

Файлы: src/types.ts, src/data/fish.ts, src/data/food.ts (нов), src/sim/food.ts (нов), src/main.ts, src/ui/panels.ts, src/save.ts, src/style.css.

Критерий: кормление не тратит деньги, а расходует запас корма; живые корма портятся; разные рыбы едят разный корм; в магазине отдельный раздел с фильтрами и сортировкой.

---------------------------------------------------------------
## 0.26.1 - Заметная порча живого корма
Раньше испортившийся корм просто исчезал со склада (только строка в журнале).

Изменения:
- При порче — flash «Живой корм испортился на складе!» + лог с названиями и количеством порций по каждому корму.
- Повторная покупка живого корма сбрасывает storedDays (свежая партия), чтобы старая партия не «протухала» добавленные свежие порции.

Файлы: src/main.ts.

Критерий: игрок сразу видит, что живой корм испортился; дозакупка живого корма не портит его мгновенно.

---------------------------------------------------------------
## 0.27.0 - Условия хранения живого корма
Живой корм (артемия, мотыль, дафния) — скоропорт: без холодильника он испортится к концу дня покупки.

Изменения:
- В «Обустройство → Оснащение» добавлен холодильник (250₽).
- С холодильником живой корм хранится свой срок годности (shelfLifeDays); без него портится к концу дня.
- Предупреждения: при покупке живого корма без холодильника (flash + лог), в модалке кормления и на складе («портится к концу дня (нет холодильника)»), в карточке магазина — подсказка купить холодильник.
- Состояние холодильника сохраняется (ShopState.fridge), миграции для старых сейвов.

Файлы: src/types.ts, src/save.ts, src/sim/food.ts (liveShelfDays), src/main.ts (onBuyShopItem, advanceFoodSpoilage, onBuyFood), src/ui/panels.ts.

Критерий: без холодильника живой корм не доживает до следующего дня; с холодильником живёт по сроку годности.

---------------------------------------------------------------
## 0.27.1 - Тост-уведомления без сдвига верстки
Flash-сообщения были in-flow элементом внутри .hud-right: длинные тексты меняли его ширину, и flex-wrap шапки с space-between «ехала».

Изменения:
- .hud-flash перемещён из hud в корень и стал position: fixed — всплывает поверх контента и не занимает места в потоке.
- Автоскрытие переведено с проверки времени в update() на собственный setTimeout (2.6 с).

Файлы: src/ui/panels.ts, src/style.css.

Критерий: уведомления не меняют ширину/строку хедера при показе и скрытии.

---------------------------------------------------------------
## 0.28.0 - Наглядные заказы в зале
Заказ теперь виден, не открывая вкладки.

Изменения:
- Касса (при купленной cashRegister) рисуется на канвасе зала справа у входа: при ожидающих заказах мигает жёлтым «✦ N», при нуле зелёная. Клик по кассе открывает вкладку «Заказы».
- DOM-баннер «✦ N заказов ожидают — открыть» поверх сцены зала (пульсирует, скрыт при 0).
- hud «Заказов: N» теперь кликабельный → вкладка «Заказы».

Файлы: src/ui/renderHall.ts (layoutCashRegister/drawCashRegister), src/main.ts (клик кассы), src/ui/panels.ts (баннер, клик hud, fpZal), src/style.css.

Критерий: заказ виден из зала (касса + баннер + кликабельный hud), клик по любому из них открывает заказы.

---------------------------------------------------------------
## 0.29.0 - Архетипы покупателей
Покупатели разделены на два типа.

Изменения:
- «На месте» (kind display): выбирает вид из выставленных в зале аквариумов; наценка зависит от качества витрины.
- «Ищет конкретное» (kind demand): просит любой вид из всего каталога, даже если его нигде нет в магазине; количество 1-3, может превышать имеющийся остаток; наценка выше (1.05-1.3).
- Лог прихода: «Покупатель ищет конкретное / выбрал по витрине: …»; бейдж заказа обновлён.

Файлы: src/sim/buyers.ts (generateOrder), src/main.ts (лог), src/ui/panels.ts (бейдж).

Критерий: «ищущий конкретное» может запросить вид, которого у магазина нет и никогда не было.

---------------------------------------------------------------
## 0.30.0 - Поставщик, доставка и кислородные пакеты
Покупка рыбы «в никуда» убрана: рыбу привозит поставщик, и её нужно пересадить в аквариум.

Изменения:
- «Купить на склад» заменено на «Заказать у поставщика»: оплата сразу, рыба прибывает на следующий день (state.purchases, поле rriveDay).
- Прибывшая рыба лежит в кислородных пакетах (StockItem.bagDays, по умолчанию 3 дня); каждый день счётчик убывает, при 0 рыба гибнет (лог + flash «Рыба погибла в пакетах»).
- Заказ у поставщика требует свободной ёмкости аквариумов (reeStockSpace) — иначе flash «Нет места в аквариумах, чтобы принять рыбу».
- Склад-рыба хранится партиями (несколько записей одного вида с разной свежестью); списание идёт со старейших партий; опт продаёт все партии вида.
- UI: блок «В пути от поставщика» в магазине (День прибытия), строка «в пакетах D дн.» на складе, пометка «В пути» в карточке вида.
- Миграции: старые сейвы получают purchases: [] и agDays: 3 для существующего склада.

Файлы: src/types.ts, src/data/shop.ts (STOCK_BAG_DAYS), src/save.ts, src/sim/aquarium.ts (freeStockSpace), src/main.ts (onOrderFromSupplier, settlePurchases, advanceBags), src/ui/panels.ts, src/style.css.

Критерий: купленная рыба появляется на складе на следующий день в пакетах; без пересадки за 3 дня гибнет; без свободной ёмкости заказ не оформляется.

---------------------------------------------------------------
## 0.31.0 - Продажные аквариумы и сценарные объёмы
Реалистичный источник розничной продажи рыбы.

Изменения:
- Новый флаг аквариума «продажный» (forSale): рыбу можно продавать в розницу только из таких аквариумов. Включить можно только при decor=0; в продажный нельзя ставить декор.
- Экран аквариума: статус-строка + кнопка «Сделать продажным / Убрать из продажи» (aq-actions).
- Сценарные объёмы: в продажном аквариуме рыба занимает SALE_VOLUME_FACTOR (0.4) × minVolume, в обычной витрине — полный minVolume. canStock/usedVolume/freeStockSpace учитывают флаг.
- Розничные заказы рыбы (itemStock/takeItems → fishRetailStock/takeSellableFish) берут только особей из продажных танков; опт (wholesale) остаётся из пакетов склада.
- Продажные танки не считаются витринами: не влияют на привлекательность зала и «по витрине»-заказы.
- На канвасе зала продажные аквариумы помечены мигающей меткой «ПРОДАЖА».
- Миграции: новые/существующие аквариумы получают forSale=false.

Файлы: src/types.ts, src/save.ts, src/sim/aquarium.ts (SALE_VOLUME_FACTOR, fishRetailStock, takeSellableFish), src/sim/buyers.ts, src/main.ts (onToggleForSale, itemStock/takeItems), src/ui/panels.ts, src/ui/renderHall.ts, src/style.css.

Критерий: продать рыбу в розницу можно только из голого продажного аквариума; декорированные витрины не продаются; в продажном аквариуме помещается больше рыбы (меньший объём на особь).

## 0.31.1 - Автоматический «продажный» аквариум (без декора)

Исправление: продажный статус больше не требуется включать переключателем — он выводится автоматически из наличия декора.

Изменения:
- «Продажный» аквариум = любой аквариум без декора (isSellable: decor.length===0). Все проверки (fishRetailStock, takeSellableFish, canStock/usedVolume/volume-фактор, витрины, маркер «ПРОДАЖА») используют производный признак.
- Убран переключатель «Сделать продажным / Убрать из продажи» и action onToggleForSale — больше не нужен и сбивал с толку (кнопка «Продать» оставалась disabled, даже когда рыба уже стояла в голом аквариуме, но флаг не выставлялся).
- Поле AquariumState.forSale удалено из types.ts, save.ts (defaultState, migrateDisplayTanks) и всех мест использования; старые сохранения по-прежнему работают (поле игнорируется).
- Добавление декора в голый аквариум с рыбой валидируется: перевод в видовой режим переключает объём на полный minVolume, поэтому при переполнении блок (decorateOverflows) с подсказкой.
- Эффект: удаление декора из аквариума сразу делает его продажным.

Файлы: src/types.ts, src/save.ts, src/sim/aquarium.ts (isSellable), src/sim/buyers.ts, src/main.ts (decorateOverflows, onAddDecor/onPlaceDecorFromRack), src/ui/panels.ts (fpAq, aqInfo, aqActions).

Критерий: рыба, стоящая в любом голом аквариуме (decor.length===0), мгновенно учитывается в розничном запасе и кнопка «Продать» в заказе становится активной; декорированные витрины и рыба в пакетах склада в розницу не продаются.

## 0.31.2 - Плавающий индикатор поставок (без сдвига сетки магазина)

Исправление из списка запланированных улучшений (п.1): действия игрока не должны сдвигать уже отображённые элементы.

Изменения:
- Блок «В пути от поставщика» больше не вставляется в начало сетки рыб (`storeGrid.prepend`) — он занимал целую строку и после каждой покупки (ре-рендер по деньгам/заказам) сдвигал карточки рыб и кнопку, мешая повторным кликам.
- Вместо этого — плавающий индикатор `.purchases-pill` (position:fixed, верхний правый угол), видимый на вкладке «Магазин», пока есть заказы у поставщика. На раскладку не влияет. Обновляется по fingerprint, без пересоздания DOM каждый кадр.
- Пер-карточная подпись «В пути: N · День X» на карточках рыб сохранена.

Файлы: src/ui/panels.ts (renderPurchasesPill, удалён prepend в renderStoreFish), src/style.css (.purchases-block → .purchases-pill).

Критерий: повторные клики по кнопке «Заказать у поставщика» не смещают карточки; сводка поставок видна плавающим индикатором без влияния на раскладку.

## 0.32.0 - Покупка рыбы по количеству: двухколоночный экран

Пункт 2 плана: игрок указывает количество рыбы к заказу у поставщика.

Изменения:
- Вкладка «Магазин → Рыбы» переделана на два столбца: слева — список видов (только название + маркер + бейдж «✈ N» в пути), справа — подробная карточка выбранного вида (латиница, бейджи, параметры воды, спрос, цены закупка/розница/опт) и форма заказа.
- Форма заказа: числовой ввод количества + ползунок, ограниченные текущим балансом (max = floor(money / цена за шт)), живой итог «Итого: Y ₽», кнопка «Заказать N шт — Y ₽». При недостатке денег даже на 1 шт — пояснение и заблокированные контролы.
- onOrderFromSupplier получил аргумент count; проверка свободного места в аквариумах при заказе снята (перенесена на п.3), осталась оплата.
- Сводка «В пути: N · День X» сохранена: бейдж в списке + плавающий индикатор (п.1).

Файлы: src/ui/panels.ts (renderStoreFish → .store-fish-list/.store-fish-detail), src/style.css, src/main.ts (onOrderFromSupplier), сигнатура UIActions.

Критерий: игрок задаёт количество ползунком или вручную (предел — деньги), видит итоговую сумму и делает заказ за нужное число; отсутствие аквариума больше не блокирует заказ.

## 0.33.0 - Сворачиваемые карточки информации об аквариуме

Пункт 3 плана: разделить поток данных в панели аквариума на компактные сворачиваемые секции.

Изменения:
- Верхняя сводка: расположение (помещение · стойка) и статус «Продажный / Видовой» — вверху, больше не смешивается с параметрами.
- Карточки-аккордеоны: Вода, Обитатели, Оборудование, Декор, Дизайн. Заголовок кликабельный (chevron ▸/▾); в свёрнутом виде показывается резюме в одну строку, в развёрнутом — детали.
- Вода: в свёрнутом виде T/pH/GH/O₂ + вердикт-чип «в норме» (зелёный) / «вне нормы: …» (жёлтый, список параметров). Развёрнутое тело подсвечивает строки с параметрами вне диапазонов обитающих видов (класс .off). Без рыбы — нейтральная подсказка.
- Обитатели: свёрнуто — «N ос. · M вид.» + чип худшего здоровья; развёрнуто — группы по видам (точка-цвет, название ×количество, здоровье %).
- Оборудование: свёрнуто — названия через запятую; развёрнуто — построчно с количеством (×N).
- Декор: свёрнуто — растительность % и число объектов; развёрнуто — растительность + объекты по видам.
- Дизайн: свёрнуто — уровень и стоимость улучшения; развёрнуто — кнопка «Улучшить».
- Состояние раскрытия карточек хранится в aqCardOpen (Set) между кадрами: не сбрасывается при перерисовке по тикам, переименовании и пр.

Файлы: src/ui/panels.ts (renderAquarium + локальные helper'ы chip/outOfRange/aqCard), src/style.css (.aq-card, .aq-card-head, .aq-card-body, .aq-card-sum, .chip, .aq-line.off и др.).

Критерий: весь блок помещается в aside без простыни текста; любая секция сворачивается/разворачивается кликом по заголовку, при этом имя аквариума и редактирование не слетают; отклонённые параметры воды визуально выделены.

## 0.34.0 - Индикатор вместимости аквариума

Пункт 4 плана: наглядный показ занятых и свободных литров.

Изменения:
- В верхнюю сводку блока информации (перед карточками) добавлена полоса вместимости: прогресс-бар занятых литров + текст «Ёмкость: X / Y л» слева и «свободно Z л» справа.
- X = usedVolume(aq) (фактор SALE_VOLUME_FACTOR учитывается — продажный аквариум более плотный).
- Ширина заполнения = min(100, X/Y·100). Цвет полосы отражает заполненность: зелёный (< 80 %), жёлтый (≥ 80 %), красный (полностью / ≥ 100 %).
- Полоса всегда видна (не сворачивается); показываются только литры, без оценки числа рыб.

Файлы: src/ui/panels.ts (импорт usedVolume + блок .aq-capacity в renderAquarium), src/style.css (.aq-capacity, .capacity-track, .capacity-fill и состояния ok/high/full).

Критерий: игрок сразу понимает, насколько полон аквариум в литрах и сколько места осталось, глядя на полосу и подпись; у переполненных — красная полоса.

## 0.35.0 - Переработка механики заказа у поставщика (агрегированный прогноз)

Пункт 5 плана: правило допуска к заказу переработано — вместо блокировки по месту (неверной для старта без аквариума) — неблокирующий агрегированный прогноз вместимости.

Изменения:
- Заказ у поставщика больше не блокируется отсутствием свободного места: можно заказывать даже без аквариума (рыба прибывает на следующий день в пакеты на склад — settlePurchases/advanceBags).
- Новый хелпер orderForecast(state, speciesId, qty) в src/sim/aquarium.ts: суммарный литраж всех аквариумов, свободный объём (usedVolume с учётом SALE_VOLUME_FACTOR), объём поставок в пути (вид × qty) и сколько не хватает на конкретный заказ (needMoreL).
- В форме заказа (правый столбец вкладки «Магазин → Рыбы») — постоянная шкала-прогноз «X / Y л занято» + «N л свободно / не хватает всего», обновляется при изменении количества; при переполнении подсказка «не хватит N л — часть рыбы будет ждать пересадки в пакетах на складе» и warning-рамка.
- Прогноз не блокирует: при нажатии «Заказать», если суммарно не помещается, показывается флеш-предупреждение (заказ всё равно оформляется).

Файлы: src/sim/aquarium.ts (OrderForecast), src/ui/panels.ts (fx-box/syncCapacity в форме заказа), src/main.ts (флеш в onOrderFromSupplier), src/style.css (.fx-box, .cap-note, .aq-capacity.warn).

Критерий: игрок может заказать рыбу без аквариума; при переполнении видит шкалы и предупреждение о нехватке, но оформление не блокируется.

## 0.36.0 - Больше стартового оборудования

Пункт 6 плана: на складе для тестового периода было только 4 из 8 единиц, не было светильника и CO₂.

Изменения:
- Стартовый склад оборудования rackInventory пополнен: добавлены Освещение (light) и CO₂-дозатор (co2).
- Теперь 6 из 8 слотов стеллажа: {'heater','thermometer','airPump','filter','light','co2'}.
- Касается только новых игр; существующие сохранения сохраняют свой инвентарь (миграция в src/save.ts не перезаписывает его).

Файл: src/save.ts.

Критерий: в новой игре на складе сразу доступны светильник и CO₂, чтобы тестировать свет и растительность без покупки.
